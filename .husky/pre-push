#!/bin/sh

echo "Running pre-push checks..."

# Change to excalidraw-ui directory for checks
cd excalidraw-ui || exit 1

# Run typecheck
echo "Type checking..."
yarn test:typecheck || exit 1

echo "All checks passed!"
